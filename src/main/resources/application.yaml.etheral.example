# Camunda BPM database configuration
spring.datasource.url: jdbc:h2:file:./camunda-h2-database

# Camunda BPM process location
deployment-resource-pattern: classpath*:/bpmn/, classpath:/forms/*

# Camunda BPM configuration
camunda.bpm.admin-user:
  id: Admin
  password: pas@23

# SMTP mail configuration
spring.mail.host: smtp.ethereal.email
spring.mail.port: 587
spring.mail.username: #hier kommt die Etheral Email rein
spring.mail.password: #hier kommt das Etheral Passwort rein
  spring.mail.properties.mail.smtp.auth=true
  spring.mail.properties.mail.smtp.starttls.enable=true